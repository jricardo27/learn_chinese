{
  "name": "magic",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "sync": "rm -rf www && mkdir -p www && cp index.html script.js style.css www/ && cp -R audio images www/ && find . -maxdepth 1 -name 'words_*.js' -exec cp {} www/ \\; && npx cap sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/cli": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/ios": "^8.0.2"
  }
}