{
  "name": "magic",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "sync": "mkdir -p www && rm -rf www/* && cp index.html script.js style.css words_data.js words_analysis.js www/ && cp -R audio images www/ && npx cap sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/cli": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/ios": "^8.0.2"
  }
}